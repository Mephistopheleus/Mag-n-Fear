# Core Dependencies
asyncio-throttle==1.0.2
aiohttp==3.14.1
websockets==12.0
uvloop==0.19.0  # Для ускорения asyncio на Linux (для Windows будет использоваться стандартный event loop)

# Data Processing
polars==0.20.3  # Быстрая обработка данных
numpy==1.26.3
pandas==2.1.4

# Configuration & Validation
pydantic==2.5.3
pyyaml==6.0.1

# Math & Statistics
scipy==1.12.0
scikit-learn==1.3.2  # Только для статистических моделей (кластеризация, регрессия), без нейросетей

# Optimization (для Auto-Tuner)
optuna==3.5.0

# Storage
aiosqlite==0.19.0  # Асинхронная работа с SQLite
pyarrow==15.0.0  # Для быстрых снимков данных (Parquet)

# Logging & Monitoring
loguru==0.7.2
rich==13.7.0  # Красивые логи в консоль

# Exchange Connectivity
ccxt==4.2.2  # Поддержка множества бирж

# Testing (опционально)
pytest==7.4.4
pytest-asyncio==0.23.3
